[project]
name = "wordle"
version = "0.1.0"
description = "Wordle Solver"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.2.2",
    "scipy>=1.15.1",
    "tqdm>=4.67.1",
]
